// for string
#include <iostream>
using namespace std;

//---------------------------------------------------------------------------

struct column{
	string name;
	string datatype;
	string values[10000];
};

struct table{
	int cols_n;
	int rows_n;
	column cols[100];
};

extern struct table db, shown_db, buf_db;
